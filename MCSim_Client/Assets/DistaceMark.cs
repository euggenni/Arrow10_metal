using UnityEngine;

public class DistaceMark : MonoBehaviour {
    public int distance;

    public int getDistence() {
        return distance;
    }

    // Use this for initialization
    void Start() {
    }

    // Update is called once per frame
    void Update() {
    }
}